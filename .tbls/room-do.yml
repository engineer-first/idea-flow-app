name: RoomDO (room内部)
desc: >-
  ルーム1つにつき1つ生成される SQLite-backed Durable Object の内部スキーマ。
  メンバーシップと付箋の確定状態の真実をここで持つ。D1 の rooms.id はこの
  DO インスタンスに対応するが、DBレベルでの結合は存在しない（アプリケーション
  コード上でのみ対応づけられる）。D1 側は .tbls/d1.yml を参照。

dsn: sqlite://tmp/room-do-schema.sqlite
docPath: schema-diagrams/room-do

# schema_migrations（適用済みマイグレーションIDの記録）は版管理用の内部
# テーブルでドメインモデルではないため除外する。schema_version は旧・件数
# 方式の名残で、変換前のストレージにのみ存在する。
exclude:
  - schema_migrations
  - schema_version

er:
  format: svg

# tbls lint（npm run db:schema:lint / CI で実行）。ルール選定の理由は
# .tbls/d1.yml のコメントを参照。RoomDO は現状宣言的な FK を持たないため
# requireForeignKeyIndex は今は何も検出しないが、将来 FK を導入したときの
# インデックス貼り忘れを防ぐガードとして先に置いておく。
lint:
  requireForeignKeyIndex:
    enabled: true
  columnCount:
    enabled: true
    max: 12
  duplicateRelations:
    enabled: true
