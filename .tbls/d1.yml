name: D1 (lobby)
desc: >-
  招待コード → ルーム解決のためのディレクトリ。ルームの中身（メンバー・付箋）の真実は
  RoomDO が持つため、ここには含まれない。RoomDO 側は .tbls/room-do.yml を参照。
  D1 と RoomDO は物理的に別ストレージであり、DBレベルでの結合は存在しない。

dsn: sqlite://tmp/d1-schema.sqlite
docPath: docs/schema/d1

er:
  format: mermaid
